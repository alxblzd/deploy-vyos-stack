---
# Configure DNS services only (DHCP disabled - using static IPs)

- name: Configure DNS forwarding
  include_tasks: dns.yml

# DHCP is disabled per requirements - all VMs use static IPs
# - name: Configure DHCP servers
#   include_tasks: dhcp.yml

- name: Save services configuration
  vyos.vyos.vyos_config:
    save: true
